{
  "name": "codelookup",
  "version": "0.5.0",
  "description": "CodeLookup installer - detects your AI coding agents and installs codelookup for each one.",
  "license": "MIT",
  "bin": {
    "codelookup": "dist/install.js",
    "codelookup-build": "dist/generate-graph.js",
    "codelookup-check": "dist/pre-check.js"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist/",
    "skills/",
    "README.md",
    "INSTALL.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepack": "npm run build",
    "test": "npm run build && node dist/generate-graph.js && node dist/pre-check.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
